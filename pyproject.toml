[project]
name = "traitors"
version = "0.0.1"
description = "A paper on the traitors"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
    "stet>=0.0.2",
    "dvc>=3.0",
    "backstab>=0.1.0",
]

[tool.uv]
package = false
